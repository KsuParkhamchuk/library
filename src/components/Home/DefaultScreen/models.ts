export namespace DefaultScreenModel {
    export interface State {
        popularBooks: Array<any>,
        genres: Array<any>,     
        books: Array<any>,
        nonFictionBooks: Array<any>,
        childBooks: Array<any>,
        isAuthorized: boolean
    }

    export interface Props {
       
    }
}